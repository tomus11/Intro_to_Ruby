puts "Plz entah ya numba"
num = gets.chomp.to_i

if (num > 0 && num <= 50)
  puts "Your numba is less than 50"
elsif (num > 51 && num < 100)
  puts "Your numba is between 51 and 100"
elsif num > 100
  puts "to infinity and beyond"
else
  puts "No negatives sir"
end
